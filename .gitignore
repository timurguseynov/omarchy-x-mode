### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon


# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.aider*

# Build artifacts
*.so
*.o
build/

.pi/sessions

# Scratch scenario for a one-off look while working on a test. Not part of the
# suite and never committed; if it is left behind it must not reach a commit.
tests/nest/debug_tmp_test.sh
tests/nest/integration/debug_tmp_test.sh

# Test working dir (nest state, logs, socket)
tests/.nest

_docs
